<tr>
    <td hidden > <%= data.id_usuarios%> </td>
    <td> <%= data.concatenar%> </td>
    <td> <%= data.cedula%> </td>
    <td> <%= data.nombre_coordinador%> </td>
    <td> <%= data.correo%> </td>
    <td> <%= data.telefono%> </td>
    <% if (data.rol===1) { %>
      <td>Administrador</td>
      <% } else { %>
        <td>General</td>
        <% } %>
          <td>
            <button type="button" data-bs-toggle="modal" data-bs-target="#modalEliminar<%=data.id_usuarios%>"
              class="btn btn-outline-danger m-1 btnDelete" title="Eliminar Usuario">❌</button>

            <div class="modal fade" id="modalEliminar<%=data.id_usuarios%>" tabindex="-1"
              aria-labelledby="exampleModalLabel" aria-hidden="true">
              <div class="modal-dialog">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Eliminar Usuario</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                  </div>
                  <div class="modal-body">
                    <h3>Confirmar Eliminación:</h3>
                    <div class="mb-3">
                      <label for="recipient-name" class="col-form-label">Cedula</label>
                      <input readonly value="<%= data.cedula%> ">
                    </div>
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <input type="submit" value="aceptar" class="btn btn-primary btnAceptar">
                  </div>
                </div>
              </div>
            </div>
            <button type="button" title="Editar" class="btn btn-outline-success btnEdit" data-bs-toggle="modal"
              data-bs-target="#exampleModal<%=data.id_usuarios%>" data-bs-whatever="@fat">🖋</button>
          </td>
  </tr>

  <div class="modal fade" id="exampleModal<%=data.id_usuarios%>" tabindex="-1"
    aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">MODIFICA EL CAMPO REQUERIDO</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <form class="usersForm">

            <div class="mb-3">
              <label for="recipient-name" class="col-form-label">Cedula</label>
              <input type="text" name="cedula" value="<%= data.cedula%>">
            </div>
            
            <div class="mb-3">
              <label for="recipient-name" class="col-form-label">Nombre Coordinador</label>
              <input type="text" name="nombre_coordinador" value="<%= data.nombre_coordinador%>">
            </div>

            <div class="mb-3">
              <label for="recipient-name" class="col-form-label">Correo</label>
              <input type="text" name="correo" value="<%= data.correo%>">
            </div>
            <div class="mb-3">
              <label for="recipient-name" class="col-form-label">télefono</label>
              <input type="number" name="telefono" value="<%= data.telefono%>">
            </div>

            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"> Cancelar</button>
              <input type="submit" class="btn btn-primary" value="Aceptar">
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>